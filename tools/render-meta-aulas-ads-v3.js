const { chromium } = require('playwright');
const path = require('path');
(async()=>{const root=path.resolve(__dirname,'..');const dir=path.join(root,'output','meta-ads','aulas-particulares');const browser=await chromium.launch({headless:true,executablePath:'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe'});for(const item of [{file:'c01-dax-travado.html',slug:'c01-dax-travado'},{file:'c02-aula-que-voce-precisa.html',slug:'c02-aula-que-voce-precisa'}]){for(const format of [{name:'feed',w:1080,h:1350,q:''},{name:'story',w:1080,h:1920,q:'?story=1'}]){const page=await browser.newPage({viewport:{width:format.w,height:format.h},deviceScaleFactor:1});await page.goto('file:///'+path.join(dir,item.file).replace(/\\/g,'/')+format.q);await page.screenshot({path:path.join(dir,`${item.slug}-${format.name}-1080x${format.h}.png`),fullPage:false});await page.close();}}await browser.close();})();
